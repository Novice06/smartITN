<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout pour MainActivity.java
    Écran de saisie du numéro matricule (student_id)
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="#F5F7FA">

    <!-- Logo / Titre -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ITN Mobile"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="#1A237E"
        android:layout_marginBottom="8dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tableau de bord étudiant"
        android:textSize="16sp"
        android:textColor="#5C6BC0"
        android:layout_marginBottom="48dp"/>

    <!-- Champ matricule -->
    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Numéro matricule (ID)"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
        android:layout_marginBottom="24dp">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/et_matricule"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="number"
            android:maxLines="1"
            android:imeOptions="actionDone"/>

    </com.google.android.material.textfield.TextInputLayout>

    <!-- Bouton accéder -->
    <Button
        android:id="@+id/btn_acceder"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="ACCÉDER AU TABLEAU DE BORD"
        android:textSize="14sp"
        android:backgroundTint="#1A237E"
        android:layout_marginBottom="16dp"/>

    <!-- Indicateur de chargement (caché par défaut) -->
    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"/>

</LinearLayout>
